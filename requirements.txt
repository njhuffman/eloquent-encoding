# Chess programming & ML stack
numpy>=1.24.0
python-chess>=1.999
h5py>=3.8.0
torch>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Lichess DB pull script
zstandard>=0.21.0
requests>=2.28.0
tqdm>=4.65.0
pyyaml>=6.0
psutil>=5.9.0

# Experiment tracking (W&B). Only used when a model spec has a `wandb` block.
wandb>=0.16.0
