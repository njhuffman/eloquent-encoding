{
  "name": "Eloquence (Chess / Python)",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--gpus", "all", "-p", "8765:8765"],
  "forwardPorts": [8765],
  "portsAttributes": {
    "8765": {
      "label": "JEPA training dashboard",
      "onAutoForward": "notify"
    }
  },
  "mounts": [
    "source=eloquence-bulk,target=/mnt/eloquence_bulk,type=volume"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "remoteUser": "vscode",
  "postCreateCommand": "sudo mkdir -p /mnt/eloquence_bulk/jepa_checkpoints /mnt/eloquence_bulk/databases && sudo chown -R vscode:vscode /mnt/eloquence_bulk && pip install --no-cache-dir -r requirements.txt && pip install --no-cache-dir -r requirements-dashboard.txt"
}
